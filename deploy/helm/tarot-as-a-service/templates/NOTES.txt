Tarot-as-a-Service has been deployed!

{{- if .Values.ingress.enabled }}

The API is available at:
{{- if .Values.ingress.tls.enabled }}
  https://{{ .Values.ingress.host }}
{{- else }}
  http://{{ .Values.ingress.host }}
{{- end }}

{{- else }}

To access the API, run:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "tarot-as-a-service.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:{{ .Values.service.targetPort }}

Then visit: http://localhost:8080/healthz

{{- end }}

Health check endpoint: /healthz

API endpoints:
  - GET /v1/tarot?q=<question>&n=<cards>&deck=<deck_id>&spread=<type>
  - GET /healthz

Configuration:
  - LLM Model: {{ .Values.env.LLM_MODEL }}
  - LLM Provider: {{ .Values.env.LLM_PROVIDER }}
  - LLM Timeout: {{ .Values.env.LLM_TIMEOUT }}
